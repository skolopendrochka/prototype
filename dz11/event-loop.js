console.log('1. Начало синхронного кода');

// Макрозадача через setTimeout
setTimeout(() => {
  console.log('4. Этот код выполнится после всех микрозадач и текущего стека');
}, 0);

// Микрозадача через Promise
Promise.resolve()
  .then(() => {
    console.log('3. Микрозадача из Promise (выполнится перед макрозадачами)');
  });

// Синхронный код
console.log('2. Конец синхронного кода');

/*
Порядок выполнения:
1. Сначала выполняется весь синхронный код (1 и 2)
2. Затем выполняются все микрозадачи (3) из очереди Promise
3. Затем выполняются макрозадачи (4) из очереди setTimeout
*/
